server:
  port: 8084

spring:
  application:
    name: registro

  # BD INDEPENDIENTE de registro (base "registrodb" en Neon)
  datasource:
    url: ${REGISTRO_DB_URL:jdbc:postgresql://ep-mute-band-anlf1eyt-pooler.c-6.us-east-1.aws.neon.tech/registrodb?sslmode=require}
    username: ${REGISTRO_DB_USER:neondb_owner}
    password: ${REGISTRO_DB_PASS:npg_TrxfdWBpS47F}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

# Servicio que consume registro vía WebClient
services:
  ventas:
    url: ${VENTAS_URL:http://localhost:8083}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
